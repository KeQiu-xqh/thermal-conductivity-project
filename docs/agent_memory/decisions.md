# Decisions

更新：2026-04-17

## 实验路线

- 主线采用：**智能加热平台 + 金属丝 / 金属圆杆样品 + Thermal-90 热像采集**
- 金属条不再作为正式主样品，只保留为调试或备选

## 数据路线

- 原始热像数据保留为本地实验资产，不纳入 git
- 训练数据从原始数据构建，不直接把普通视频当作源数据
- 当前先用固定矩形 ROI 构建训练数据，后续再收敛到正式样品 ROI

## 仓库整理路线

- `code/` 只保留当前正式运行脚本
- `code_appendix/` 只保留讲义附录和参考副本
- 讲义原稿迁入 `docs/references/`
- 原始数据、派生数据、GIF、PNG 全部放入专门目录并默认不跟踪 git
