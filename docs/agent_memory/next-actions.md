# Next Actions

更新：2026-05-05

1. 按修订方案确认正式器材补充项，至少落实哑光黑漆、导热硅脂和根部压紧方案。
2. 固定正式金属棒样品 ROI，并将训练数据从 `x, y, t -> T` 收敛为适合 1D 反演的 `x, t -> T`。
3. 为正式实验补充环境与边界元数据：`T_inf`、样品直径、有效长度、加热端长度、表面处理方式、材料参数来源。
4. 先采一组黑体化后的正式数据，验证热像温度场是否稳定、是否可沿棒轴提取平滑曲线。
5. 基于修订后的 PDE 与真实边界条件，开始实现联合学习 `alpha` 和 `h` 的 PINN 第一版。
6. 用已到位的导热硅脂做一次热端装配测试：加热台表面清洁 -> 薄涂硅脂 -> 金属棒热端压紧 -> 采一组短时热像，检查热端附近温度场是否更稳定。
7. 若热端仍出现明显跳变或局部热点，优先排查三项：接触面平整度、硅脂是否过厚、压紧力是否不足或不稳定。
8. 基于 `thermal90-20260511-run01.dat` 的预览结果，重新设计正式录制起点：先开录，再开始加热或放置样品，避免只录到接近平衡或冷却阶段。
9. 保持本次可见热区的大致画面布局，但进一步固定相机、金属棒和热端接触，减少热区位置漂移和整体温度小幅抖动。
10. 下一组正式短时验证数据的目标不再只是“看到热区”，而是要看到沿棒方向更清晰的温度梯度，以及随时间更明确的升温演化。
11. 把 `thermal90-20260511-run01.dat` 单列为“冷却验证数据”，不要混入正式升温响应数据集。
12. 如果后续目标是验证红外绝对测温是否可信，优先安排一组“同位置热像 + 接触式温度计/热电偶”对照。
13. 如果后续目标是做导热反演，仍需重新采一组从更明确加热起点开始的正式升温数据。
14. 若继续做热像定量分析，先补一条标定链：确认铝棒直径或在画面中放入已知尺寸参照，用于把像素长度换算成毫米。
15. 对正式 PINN 数据，优先保留“从加热开始的升温序列”和“从取下后的冷却序列”各一类，不要混合解释。
16. 若要验证绝对温度可信度，安排一组“热像 + 接触式测温/热电偶”的同步对照，重点检查金属表面低发射率导致的低估误差。
