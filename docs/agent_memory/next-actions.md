# Next Actions

更新：2026-05-05

1. 按修订方案确认正式器材补充项，至少落实哑光黑漆、导热硅脂和根部压紧方案。
2. 固定正式金属棒样品 ROI，并将训练数据从 `x, y, t -> T` 收敛为适合 1D 反演的 `x, t -> T`。
3. 为正式实验补充环境与边界元数据：`T_inf`、样品直径、有效长度、加热端长度、表面处理方式、材料参数来源。
4. 先采一组黑体化后的正式数据，验证热像温度场是否稳定、是否可沿棒轴提取平滑曲线。
5. 基于修订后的 PDE 与真实边界条件，开始实现联合学习 `alpha` 和 `h` 的 PINN 第一版。
