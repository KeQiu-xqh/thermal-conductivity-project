# Next Actions

更新：2026-05-22

1. 在相同 `SK8 + 8 mm` 装配下再录一组黑体化数据，保持 `7 fps`、冷态起点和相机距离尽量一致。
2. 用同样的正式流程复现一次：
   - `ROI = x=10~80, y=30~34`
   - `initial_mode = measured`
   - `right_bc_mode = none`
3. 比较两次实验的 `alpha`、`h`、`k` 是否稳定落在同一量级。
4. 补一次外部标尺标定，替代当前单纯由棒直径得到的 `2.0 mm/px`。
5. 若后续要写正式结果，优先报告：
   - 当前 PDE 形式
   - 左边界的表观定义
   - 右边界为何不显式施加
   - 结果仍受重复性和长度标定影响
