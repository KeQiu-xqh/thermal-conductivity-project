# Next Actions

更新：2026-04-17

1. 基于 `thermal90-20260417-run01_preview.gif` 固定金属丝正式 ROI，停止继续使用“候选热区 ROI”。
2. 将训练数据从当前的 `x, y, t -> T` 版本，进一步收敛为更适合 1D 导热反演的格式。
3. 统一后续实验数据命名规则，例如 `thermal90-YYYYMMDD-runNN.dat`。
4. 再采一组正式样品数据，确保样品位置、相机位置和加热过程都固定。
5. 在 ROI 稳定后，再进入 PINN / 导热系数反演主线。
