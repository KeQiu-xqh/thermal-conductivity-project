# 实验进度

更新：2026-05-05

## 当前定位

项目已经完成从设备连接到原始热像数据回看的基础闭环，当前不再处于“环境能否跑通”的阶段，而是进入“正式样品、正式 ROI、正式模型能否形成可信反演”的阶段。

## 已完成

### 1. 采集链路

- Raspberry Pi 5 与 Thermal-90 Camera HAT 已完成基础接入。
- 官方 `stream_spi.py` 已验证可运行。
- [thermal90_ir_temp.py](/C:/Users/28146/Desktop/thermal-conductivity-project/code/thermal90_ir_temp.py) 已验证可运行。
- VNC 下的实时热像显示链路已确认可用。

### 2. 数据回传与回看

- 已成功录制真实热像原始数据 `.dat`。
- 已成功从树莓派回传到 Windows 本地。
- 已确认 `.dat` 是逐帧温度矩阵数据，不是普通视频文件。
- [view_dat.py](/C:/Users/28146/Desktop/thermal-conductivity-project/code/view_dat.py) 已可生成 GIF 与基础分析图。

### 3. 训练数据基础构建

- [construct_training_data.py](/C:/Users/28146/Desktop/thermal-conductivity-project/code/construct_training_data.py) 已可从固定矩形 ROI 构建 `npz/csv/meta.json`。
- 当前已经具备从原始热像到结构化训练数据的基础转换链路。

### 4. 局部测温稳定性验证

- `data/raw/thermal90-20260424-run02.dat` 中已出现稳定高温斜带。
- 这说明系统已经具备做局部热像测温稳定性验证的条件。

## 2026-05-05 新进展

- 已完成对原参考报告模型的物理复核。
- 已确认原有理想绝热 PINN 不能直接作为正式实验模型。
- 已确定修订方向：保留一维建模，但正式引入黑体化处理、根部热接触改进和侧向散热项。
- 项目主线从“验证 demo 级 PINN 是否能跑”切换为“搭建真实可闭环的非接触导热反演方案”。

## 当前结论

- 真实实验数据闭环已经建立：采集 -> 回传 -> 回看 -> 基础分析 -> ROI 数据构建。
- 当前主要风险不再是“采不到数据”，而是“样品 ROI、数据格式和物理模型是否一致”。
- 当前通用 `x, y, t -> T` 数据仍不是最终正式反演数据。

## 当前未完成部分

- 固定正式金属棒样品的物理 ROI。
- 完成样品黑体化与根部压紧搭建。
- 补齐环境与边界元数据。
- 将数据格式收敛为 `x, t -> T`。
- 实现修订后的 PINN 第一版并完成单样品闭环验证。
